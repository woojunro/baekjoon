-- 코드를 입력하세요
SET @MAX_PRICE = (
    SELECT
        MAX(PRICE)
    FROM
        FOOD_PRODUCT
);

SELECT
    PRODUCT_ID,
    PRODUCT_NAME,
    PRODUCT_CD,
    CATEGORY,
    PRICE
FROM
    FOOD_PRODUCT
WHERE
    PRICE = @MAX_PRICE
;