WITH UNION_TABLE AS (
    (SELECT * FROM FIRST_HALF)
    UNION ALL
    (SELECT * FROM JULY)
)

SELECT
    FLAVOR
FROM
    UNION_TABLE
GROUP BY
    FLAVOR
ORDER BY
    SUM(TOTAL_ORDER) DESC
LIMIT
    3
;